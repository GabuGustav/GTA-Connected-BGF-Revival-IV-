[build]
  command = "echo 'No build needed'"
  functions = "netlify/functions"

# SPA fallback: serve index.html for client-side routes.
# Netlify Functions at /.netlify/functions/* are served automatically (no redirect needed).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
