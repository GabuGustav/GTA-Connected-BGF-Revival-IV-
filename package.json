{
  "name": "bgf-mail-api",
  "version": "1.0.0",
  "description": "BGF Revival IV Mail API Server",
  "main": "api-server.js",
  "scripts": {
    "start": "node api-server.js",
    "dev": "nodemon api-server.js",
    "netlify": "netlify dev",
    "build:cf-pages": "node scripts/prepare-pages-site.js",
    "deploy:cf-pages": "npm run build:cf-pages && wrangler pages deploy --commit-dirty=true",
    "pages:dev": "npm run build:cf-pages && wrangler pages dev _site"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "node-fetch": "^2.6.7",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "wrangler": "^4.103.0"
  },
  "resolutions": {
    "tar": "^7.5.10"
  }
}
