# Cloudflare Pages — CI deploy: npm run deploy:cf-pages  (wrangler pages deploy, NOT wrangler deploy)
name = "bgf-revival"
compatibility_date = "2024-09-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "_site"
